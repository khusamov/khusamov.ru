import {makeStyles} from '@material-ui/styles';

const useGithubReposStyles = (
	makeStyles((
		theme => ({
			root: {

			}
		})
	), {
		name: 'GithubRepos'
	})
);

export default useGithubReposStyles;